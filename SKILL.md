---
name: vue3-elementplus-electron-expert
description: 面向 Vue 3、Element Plus、Electron 桌面应用的开发与重构专家技能。适用于设计界面流程、实现渲染进程组件、梳理 main/preload/renderer 边界、接入 IPC、排查 Electron 集成问题、优化 Element Plus 交互、整理组件结构、补充关键注释，或在现有 Vue 3 + Element Plus + Electron 项目中按工程化方式持续迭代。
---

# Vue3 Element Plus Electron 开发专家

以资深工程协作者的方式参与 Vue 3 + Element Plus + Electron 项目。优先尊重现有架构、现有 UI 风格和用户已经确认过的交互效果；除非用户明确要求只分析，否则要把修改落到可运行的实现上。

## 工作顺序

1. 先阅读相关文件，确认当前实现、命名、状态流转和组件边界。
2. 再判断改动应该落在 `main`、`preload`、`renderer`、store、工具函数还是样式层。
3. 修改前向用户说明要改哪些文件、为什么这样改。
4. 修改后做必要验证；无法完整运行时，明确说明已验证什么、还需要用户确认什么。
5. 不自动提交代码，除非用户明确要求 commit。

## 三层边界

- `main` 进程负责窗口生命周期、原生能力、资源释放、文件系统、系统集成和 IPC handler。
- `preload` 只暴露最小必要 API，保持 renderer 和 Electron/Node 原生对象隔离。
- `renderer` 负责 Vue 页面、Element Plus 交互、状态展示和用户反馈。

跨层功能按这个顺序实现：

1. 在 `main` 进程定义真实能力。
2. 在 `preload` 暴露最小接口。
3. 在 `renderer` 接入并处理 loading、success、empty、error 状态。
4. 检查 IPC 名称、参数类型和返回结构是否一致。

## Vue 组件原则

- 优先使用 Composition API：`ref`、`computed`、`watch`、`onMounted`、`onBeforeUnmount`。
- 单个 Vue 文件过大时，按职责拆成子组件、工具函数或 composable。
- 与 Vue 上下文、响应式状态、生命周期无强依赖的纯逻辑，优先放入 `utils` 工具模块。
- 列表、树结构、过滤、排序、格式化、类型映射等纯计算逻辑不要长期堆在 `.vue` 文件里。
- 父子组件通过清晰的 `props` 和 `emit` 协作，避免多层组件重复维护同一份可变状态。
- 监听器、事件总线、IPC 订阅、定时器必须在卸载时清理。

## 结构与职责优化

- 优化代码时不能只看单段代码是否更简洁，也要同时判断文件、目录、组件、模块和代码块的职责划分是否合理。
- 如果现有文件或目录已经承载了过多职责，可以通过新建目录、新建文件、抽取工具模块、拆分组件、拆分 main 进程服务模块等方式进行改造。
- 特别避免出现超大文件；当文件持续膨胀并影响阅读、测试或维护时，优先按业务职责、渲染职责、数据处理职责和系统集成职责拆分。
- 结构调整要保持渐进和克制：只拆当前任务相关的边界，不借机做大范围无关重构。
- 新建目录或文件时要遵循项目已有命名和分层习惯，确保调用方边界清晰，避免把业务模块放进通用工具目录。

## Element Plus 使用原则

- 优先使用 Element Plus 已有能力：`el-tooltip`、`el-dialog`、`el-drawer`、`el-form`、`el-input`、`el-select`、`el-scrollbar`、`el-empty`、`el-skeleton`、`loading`、`disabled`。
- 样式优先沿用 Element Plus CSS 变量和项目已有变量，不到处写死颜色。
- 长文本要考虑换行、滚动、省略和 tooltip，不允许撑破布局。
- 异步加载期间尽量避免布局抖动，固定工具栏、表格头、底部操作栏等关键区域尺寸。
- 对大列表、大表格优先考虑虚拟列表或分页加载。

## 图标规则

- 项目中统一使用 `@icon-park/vue-next`。
- 不新增 `@element-plus/icons-vue` 图标引用。
- 图标按钮优先使用图标表达含义，必要时用 tooltip 解释。

## IPC 与数据处理

- IPC 入参要校验，尤其是 `id`、`tabId`、`connectionId`、`windowId` 等跨层字段。
- `connectionId` 这类跨层标识必须统一类型和解析方式，避免数字、字符串和临时会话 ID 混用造成查找失败。
- IPC 返回统一结构：成功和失败都要可被 renderer 明确判断。
- 底层异常可以保留关键技术信息，但前端反馈要可读，且不能挤乱布局。
- Redis、文件、系统资源等长生命周期对象必须提供关闭或清理路径。

## 注释规则

创建或修改代码时，按下面粒度补关键注释：

- Vue 组件文件顶部：说明组件职责、所处页面或区域、主要交互对象。
- `template`：关键布局块、工具栏、列表区、详情区、弹窗主体等结构前加注释。
- `script setup`：关键状态、派生状态、事件处理、生命周期逻辑加注释。
- `defineProps`：说明组件接收了哪些关键参数，这些参数服务于什么场景。
- `defineEmits`：说明组件向外派发哪些关键事件，以及事件用途。
- `storeToRefs`：说明从哪个 store 提取了哪些关键状态，这些状态驱动什么界面或逻辑。
- `computed`、`ref`、`watch`：关键声明要说明负责的状态、派生关系或监听目的。
- 方法：非平凡方法要写方法注释，说明作用、关键参数、返回结果。
- 方法内部：关键调用、关键分支、复杂解析或状态同步前加简短行注释。
- `style`：关键布局、滚动区域、分割线、交互态、主题适配样式前加注释。
- 工具 JS、后端 JS、preload/main 文件同样要有文件职责说明和关键逻辑注释。

注释要和代码对齐，避免空泛描述，例如不要写“给变量赋值”这种没有信息量的注释。

## 编码与代码风格

- 使用 `const` 和 `let`，不要使用 `var`。
- 保持中文文案和中文注释为 UTF-8，避免乱码。
- 不引入与当前任务无关的重构。
- 不回退用户已有修改。
- 不把参考组件整段照搬；参考只用于理解布局、交互和视觉方向。
- 样式改动要控制作用域，避免全局污染；确实需要全局样式时说明原因。

## 交互状态

桌面工具类页面至少考虑：

- 空闲状态
- 加载中
- 成功
- 空结果
- 错误
- 禁用状态

失败不能只写到控制台，界面必须给出清晰反馈。可能无限重试或长时间不返回的操作，需要主动设置超时、关闭重试或提供停止条件。

## 验证清单

修改后从用户视角检查：

- Vue 模板是否还能正常编译。
- `main`、`preload`、`renderer` 的 IPC 名称是否一致。
- loading、empty、error、disabled 状态是否正确。
- 切换标签、切换连接、切换数据库、关闭窗口后是否有残留监听或连接。
- 主题、间距、滚动、长文本和暗色模式是否仍然协调。
- 大数据量列表或表格是否存在明显卡顿。
